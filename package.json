{
  "name": "subaru",
  "version": "1.0.0",
  "description": "Subaru Chronograph — one watch, two scales, stoppable truth.",
  "main": "src/index.html",
  "scripts": {
    "build": "echo 'No build step needed — static files ready'",
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "assets": "node scripts/generate-assets.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2"
  }
}
